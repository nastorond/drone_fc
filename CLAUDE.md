# 드론 FC 펌웨어 프로젝트

## 목표
STM32F405 기반 FC에서 비행제어 펌웨어를 직접 작성하고,
기존 Betaflight 4.2.0과 같은 조건에서 로그로 성능을 비교한다.
결과는 학위논문 또는 학술대회 논문으로 사용할 수 있다.

## 대상 하드웨어
- FC: VGOODRCF4 (제조사 VGRC), 기체 이름 mk01
- MCU: STM32F405, 168MHz (PLLP-HSE)
- IMU: MPU6000 (자이로/가속도계 겸용, SPI)
- OSD: MAX7456
- 수신기: CRSF (UART5)
- VTX: TBS SmartAudio (UART2)
- 빈 UART: 1, 3, 4, 6
- 블랙박스 저장장치 없음 (flash_info 전부 0, SD 슬롯 없음)
  → 비행 로그는 외장 시리얼 로거 필요, 벤치 단계는 USB 시리얼 출력 사용

## 대조군 (Betaflight 4.2.0)
- 빌드: 2020-06-14, 커밋 8f2d21460, MSP API 1.43
- 제어 루프: 125us (8kHz), 자이로 샘플링 8kHz
- CPU 사용률: 6% (USB 연결, 비무장 상태)
- PID (profile 0): P/I/D — pitch 34/55/19, roll 32/50/19, yaw P46 I51
  D_min은 roll/pitch 모두 0 (비활성)
- 그 외 설정은 대부분 기본값
- 이 버전은 비교가 끝날 때까지 고정한다. 업그레이드 금지.

## 폴더 규칙
- betaflight/  : 대조군 코드 (4.2.0 태그). **읽기 전용, 수정 금지**
- firmware/    : 자체 FC 펌웨어
- baseline/    : version, status, diff all 출력, 보드 사양 기록
- logs/betaflight/, logs/custom/ : 펌웨어별 비행 로그
- analysis/    : 로그 분석 스크립트, 그래프 생성
- docs/        : 시험 절차, 실험 기록, 논문 메모

## 비교 항목
- 제어 루프 주기와 지터, CPU 사용률
- 센서 입력 → 모터 출력 지연
- 호버링 자세 오차
- 외란 후 복원 시간
모든 시험은 같은 기체·배터리·프로펠러·환경에서, 여러 번 반복해 평균과 편차를 낸다.
로깅 주기는 두 펌웨어를 동일하게 맞춘다.

## 개발 방침
- 가능한 부분은 HAL 없이 레지스터 수준으로 작성한다.
- 자체 펌웨어는 낮은 루프 주기(1~2kHz)로 시작해 올린다.
  대조군 8kHz와 차이가 있으면 논문에 그대로 명시한다.
- 단계 순서: 센서 읽기+로그 → 자세 추정 → 모터 출력(프로펠러 제거)
  → 축별 제어 루프(기체 고정) → 호버링
- ESC 펌웨어는 이 프로젝트 범위 밖. FC 비행 성공 후 별도로 진행한다.
  논문에는 "모터 출력은 상용 ESC 사용"으로 명시한다.

## 주의
- 펌웨어를 보드에 굽는 명령은 실행 전에 반드시 사용자에게 확인받는다.
- 대조군 복원용 4.2.0 hex와 diff all 백업이 있는지 확인 후 진행한다.